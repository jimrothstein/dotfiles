\usepackage[top=.5in, bottom=.5in, left=.7in, right=.5in]{geometry}
%	.5in bit too small for Left margin (with holes)
\usepackage{amsmath,amssymb,amstext,amsfonts}
\usepackage{lipsum}
\usepackage{xcolor}
\usepackage{tikz}
% \usepackage{fontspec}   NOT with lualatex


\DeclareMathOperator{\E}{E}

\DeclareMathOperator{\E1}{\mathbf{E}}

%	select  font here;  be sure luaotfload-tool can find it.
%		luaotfload-tool list=times  (lists all fonts?)

% \setmainfont{tinos} worked
% \setmainfont{Arimo}  
% \setmainfont{Liberation Sans Narrow}

%\setmainfont{Roboto Light}[ItalicFont=Roboto-LightItalic]

%\setmainfont{Roboto Medium}[ItalicFont=Roboto-Italic]   seems good

\setmainfont{DejaVu Serif}
